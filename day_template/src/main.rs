use tae_aoclib2025::{solve_all_inputs};

fn main() {
    solve_all_inputs("day_XX", do_task)
}

fn do_task(input: &String) -> (i64, i64) {
    let mut result1 = 0;
    let mut result2 = 0;
    (result1, result2)
}
